demo:
  build:
    image: myapp
    dockerfile: Dockerfile
  depends_on:
    - redis
    - postgres
redis:
  image: redis:3.2.11
postgres:
  image: postgres:latest
  environment:
    - POSTGRES_HOST_AUTH_METHOD=trust
